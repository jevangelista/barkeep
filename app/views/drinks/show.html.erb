<div class="container-fluid">
	<h1 class="emotions-font larger-font type-bebas"> <%= @drink.name %></h1>
	<div class="animation">
		<%= image_tag @drink.image, class: "drink-image" %>
	</div>
</div>

<div id="recipe-container">
	    <div class="left-ingredients type-anon-pro">
	                <h2 class="type-news-cycle"> Ingredients: </h2>
	                	<ul>
	                    <% @drink.ingredients.each do |i| %>
	                        <li><p><%= i %></p></li>
	                        <% end %>
	                     </ul>
	    </div>
	    <div class="right-instructions type-anon-pro">

	            <h2 class="type-news-cycle"> Instructions: </h2>
	                    <p> <%= @drink.instructions %></p>
	    </div>
</div>